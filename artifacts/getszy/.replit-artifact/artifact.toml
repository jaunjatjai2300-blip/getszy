kind = "web"
previewPath = "/"
title = "Getszy"
version = "1.0.0"
id = "artifacts/getszy"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20306

[services.development]
run = "pnpm --filter @workspace/getszy run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/getszy", "run", "build" ]
publicDir = "artifacts/getszy/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20306"
BASE_PATH = "/"
